# OpenFlow Skill References

This directory contains reference material used by the `openflow-workflow-writer` skill.

- `api-document.md`: Workflow DSL, providers, reports, artifacts, examples, and validation mistakes.
- `cli-commands.md`: `openflow run`, resume/cache, budget, background, `list`, `inspect`, `watch`, `kill`, `validate`, and `doctor` commands and options.
- `configuration.md`: Config loading, provider settings, security settings, reporting, and precedence.
- `codex-cli-setup.md`: Codex CLI readiness, default command path, proxy notes, and real-run troubleshooting.
